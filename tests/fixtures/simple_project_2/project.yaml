# This project should parse correctly and can be used to test
# docker_invocation generation

version: '1.0'

actions:

  generate_cohort:
    run: cohortextractor:0.5.2 --database-url={database_url}
    outputs:
      highly_sensitive:
        cohort: input.csv
